apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap-template.yaml
  - vault-agent-config.yaml
  - deployment.yaml
  - hpa.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: one290/shadowchats
    newName: one290/shadowchats
    newTag: api-gateway-v0.0.0
