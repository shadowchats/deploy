apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-agent-api-gateway-sidecar
  namespace: shadowchats
data:
  vault-agent.hcl: |
    auto_auth {
      method "kubernetes" {
        mount_path = "auth/kubernetes"
        config = {
          role = "api-gateway-role"
        }
      }
    }
    template {
      source      = "/vault/templates/api-gateway-appsettings.json.tpl"
      destination = "/vault/secrets/appsettings.json"
    }
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-agent-api-gateway-init
  namespace: shadowchats
data:
  vault-agent.hcl: |
    auto_auth {
      method "kubernetes" {
        mount_path = "auth/kubernetes"
        config = {
          role = "api-gateway-role"
        }
      }
    }
    exit_after_auth = true
    template {
      source      = "/vault/templates/api-gateway-appsettings.json.tpl"
      destination = "/vault/secrets/appsettings.json"
    }
